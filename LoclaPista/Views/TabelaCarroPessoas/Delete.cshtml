@model LoclaPista.Models.TabelaCarroPessoa

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<h2>Acabar Relação</h2>

<h3>Tem Certeza de que vai Deletar?</h3>
<h2>Caso acabe essa relação você não poderá usar mais esse carro,além de caso querer essa relação denovo,terá de contatar um Adm</h2>
<div>
    <h4>Carro</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.dtaCadastro)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.dtaCadastro)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.c.placa)
        </dt>
       
        <dd>
            @Html.DisplayFor(model => model.c.placa)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("Cancelar", "Index", new { id = Convert.ToInt32(HttpContext.Current.User.Identity.Name.Split('|')[1]) })
        </div>
    }
</div>
